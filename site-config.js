/**
 * GitHub Pages (sitio en subcarpeta): nombre del repo con barra inicial, ej: '/megaofertas'
 * Raíz o uso local: deja cadena vacía.
 * Compatibilidad: también se lee ELECTROSTORE_SITE_ROOT si existe.
 */
window.MEGAOFERTAS_SITE_ROOT = '';
